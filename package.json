{
  "name": "termin",
  "version": "1.0.0",
  "description": "A script to find out when there is a window for an appointment at the Berlin Bürgeramt - because it's quite dire",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.8.2",
    "puppeteer": "^3.3.0"
  }
}
